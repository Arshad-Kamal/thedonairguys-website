// Import config to run environment validation on app start
import '../lib/config.js';

// Enable server-side rendering
export const ssr = true;

// Enable prerendering for static pages
export const prerender = true;
